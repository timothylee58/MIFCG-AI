[build]
  command    = "npm run build"
  publish    = ".next"

[build.environment]
  NODE_VERSION = "22"

# @netlify/plugin-nextjs handles SSR, API routes, and ISR
[[plugins]]
  package = "@netlify/plugin-nextjs"

[dev]
  command   = "npm run dev"
  port      = 3000
  targetPort = 3000
